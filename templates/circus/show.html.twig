{% extends 'base.html.twig' %}

{% block title %}Circus{% endblock %}

{% block body %}
    <h1>Circus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ circus.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ circus.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ circus.image }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ circus.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('circus_index') }}">back to list</a>

    <a href="{{ path('circus_edit', {'id': circus.id}) }}">edit</a>

    {{ include('circus/_delete_form.html.twig') }}
{% endblock %}
